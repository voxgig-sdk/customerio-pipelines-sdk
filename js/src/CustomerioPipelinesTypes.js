// Typed models for the CustomerioPipelines SDK (JSDoc typedefs).
//
// GENERATED from the API model: main.kit.entity.<e>.fields[] and per-op
// params (op.<name>.points[].args.params[]). Field/param types come from the
// canonical type sentinels via @voxgig/sdkgen canonToType (source of truth:
// @voxgig/apidef VALID_CANON). Annotations only — no runtime effect. Do not
// edit by hand.

/**
 * @typedef {Object} Alia
 * @property {string} previousId
 * @property {string} userId
 */

/**
 * @typedef {Object} AliaCreateData
 * @property {string} previousId
 * @property {string} userId
 */

/**
 * @typedef {Object} Batch
 * @property {Array} [batch]
 * @property {*} [context]
 */

/**
 * @typedef {Object} BatchCreateData
 * @property {Array} [batch]
 * @property {*} [context]
 */

/**
 * @typedef {Object} Group
 */

/**
 * @typedef {Object} GroupCreateData
 */

/**
 * @typedef {Object} Identify
 */

/**
 * @typedef {Object} IdentifyCreateData
 */

/**
 * @typedef {Object} Page
 */

/**
 * @typedef {Object} PageCreateData
 */

/**
 * @typedef {Object} Screen
 */

/**
 * @typedef {Object} ScreenCreateData
 */

/**
 * @typedef {Object} Track
 */

/**
 * @typedef {Object} TrackCreateData
 */

